/**
 * The web half of `traySyncTask.ts`, and empty: a browser has no shade to keep
 * in step and no background to run in. Metro picks this file for the web
 * build, which keeps `expo-task-manager` out of it.
 */
export {}
