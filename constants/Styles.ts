import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  block: {
    borderRadius: 10,
    marginHorizontal: 14,
    marginTop: 20,
  },
  item: {
    flexDirection: "row",
    alignItems: "center",
    padding: 10,
    gap: 10,
  },
  separator: {
    height: 1,
    marginLeft: 50,
  },
});
