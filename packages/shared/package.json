{
  "name": "@langx/shared",
  "version": "2.0.0",
  "private": true,
  "license": "BSD-3-Clause",
  "description": "Contract shared by @langx/api and @langx/mobile: zod schemas, language and level tables, plan limits, token rules, error codes",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./appIdentity": "./src/appIdentity.ts",
    "./appScheme": "./src/appScheme.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "echo \"@langx/shared is consumed as source\" && exit 0"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
